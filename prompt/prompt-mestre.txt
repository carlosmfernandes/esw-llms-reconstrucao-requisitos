Você vai analisar o código-fonte de um protótipo de sistema web. Com base
EXCLUSIVAMENTE no código fornecido abaixo (não presuma nada além do que o
código realmente faz), liste os requisitos funcionais e não funcionais que a
aplicação efetivamente implementa. Numere cada requisito identificado (ex:
"1. ..."), com uma frase curta descrevendo o que o sistema faz. Não inclua
requisitos que o código não implementa, mesmo que pareçam prováveis para um
sistema desse tipo. Não invente funcionalidades de backend, banco de dados ou
autenticação real caso o código não as implemente de fato.

=== index.html ===
[colar conteúdo do index.html]

=== style.css ===
[colar conteúdo do style.css]

=== app.js ===
[colar conteúdo do app.js]
